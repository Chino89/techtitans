import { TimeCustomizerPipe } from './time-customizer.pipe';

describe('TimeCustomizerPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeCustomizerPipe();
    expect(pipe).toBeTruthy();
  });
});
