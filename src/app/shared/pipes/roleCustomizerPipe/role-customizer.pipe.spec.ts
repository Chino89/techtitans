import { RoleCustomizerPipe } from './role-customizer.pipe';

describe('RoleCustomizerPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleCustomizerPipe();
    expect(pipe).toBeTruthy();
  });
});
